// next.config.mjs (DIE NEUE, KORRIGIERTE DATEI)

/** @type {import('next').NextConfig} */
const config = {
  // ... deine Konfigurationen bleiben hier gleich
  reactStrictMode: true,
};

export default config;
